# Copia este archivo a ".env" (local) o ".env.production" (build de producción)
# y ajusta la URL según dónde esté corriendo el backend.

# Desarrollo local:
VITE_API_URL=http://localhost:3000

# Producción en AWS (ejemplo, usa tu dominio real con HTTPS):
# VITE_API_URL=https://api.tudominio.com
